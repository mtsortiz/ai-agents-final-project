# Core LangChain + ecosystem
langchain>=0.2.14
langgraph>=0.2.24
langchain-community>=0.2.12

# Google Gemini + LangChain integration
langchain-google-genai>=1.0.6
google-generativeai>=0.7.2

# Vector store
chromadb>=0.5.5

# Utils
python-dotenv>=1.0.1
notion-client>=2.2.1
pydantic>=2.7

# Observabilidad
langsmith>=0.1.117

# (gRPC warnings provienen del stack de Google; normalmente no hace falta fijar grpcio,
# pero si tu entorno lo pide, descomenta la siguiente línea)
# grpcio>=1.62
